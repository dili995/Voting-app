apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    job: worker
    app: worker
spec:
  selector:
    app: worker
  ports:
    - name: web
      port: 8084
      protocol: TCP
      targetPort: 8084
  type: ClusterIP

# ---
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: worker-service-monitor
#   labels:
#     release: prometheus
#     app: prometheus
# spec:
#   jobLabel: job
#   endpoints:
#     - interval: 30s
#       port: web
#       path: /metrics
#   selector:
#     matchLabels: 
#       app: worker