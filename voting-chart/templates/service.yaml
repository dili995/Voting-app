apiVersion: v1
kind: Service
metadata:
  # name: {{ .Release.Name }}-service
  name: voting-service
  labels:
    job: voting
    app: voting
spec:
  type: NodePort
  selector:
    app: voting
  ports:
    - port: 8083  
      targetPort: 8083
      nodePort: 30004
      protocol: TCP