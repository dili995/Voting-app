apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    job: postgres
    app: postgres
spec:
  selector:
    app: postgres
  ports:
    - name: web
      port: 5432
      protocol: TCP
      targetPort: 5432
  type: ClusterIP